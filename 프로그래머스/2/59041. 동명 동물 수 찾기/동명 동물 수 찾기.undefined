/*
1. FROM: 데이터를 가져올 테이블 지정
2. WHERE: 조건을 만족하는 행만 남기기
3. GROUP BY: 남은 행들은 NAME을 기준으로 묶기
4. HAVING: GROUP BY에 조건을 거는 절
5. SELECT: 최종적으로 표시할 칼럼
6. ORDER BY: 이걸로 정렬할꺼임~
*/

SELECT 
    NAME, 
    COUNT(NAME) AS COUNT
FROM ANIMAL_INS
WHERE NAME IS NOT NULL -- 이름이 없는 동물은 집계에서 제외
GROUP BY NAME -- 이름을 기준으로 그룹화
HAVING COUNT(NAME) >= 2 -- 두 번 이상 등장한 이름만 남기기
ORDER BY NAME -- 결과는 이름 순으로 조회
;
